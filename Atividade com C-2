#include <stdio.h>

int main()
{
    int numero = 42; //variavel inteira
    int *ponteiro = &numero; 
    
    printf("Valor original de numero: %d\n", numero);
    *ponteiro = 100;
    printf("Valor de numero: %d\n", numero);

    return 0;
}
